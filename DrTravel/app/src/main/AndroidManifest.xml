<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.drtravel">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo_fix"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo_fix_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Dashboard.DashboardActivity" />
        <activity android:name=".RegisterActivity.RegisterActivity" />
        <activity android:name=".ForgetPassActivity.ForgetPassActivity2" />
        <activity android:name=".ForgetPassActivity.ForgetPassActivity" />
        <activity android:name=".LoginActivity.LoginActivity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>